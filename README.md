
# INSTRUCTIONS
## START

docker-compose up --build -d

docker attach hotel_system_app

## SPECIALS KEYS

general:

StopInput = Ctrl + K;
BackSpace = Backspace;

in menu:

RunAction = Enter;
NextAction = DownArrow;
PrevAction = UpArrow;

in pagination:

NextPage = RightArrow;
PrevPage = LeftArrow;
Exit = Escape;
